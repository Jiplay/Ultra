# Environment configuration for Docker deployment
# Copy this to .env when deploying with Docker containers

# Application Settings
PORT=8080
USE_DATABASE=true

# Database Configuration (using container names)
DB_HOST=ultra-postgres
DB_PORT=5432
DB_USER=ultra_user
DB_PASSWORD=ultra_password
DB_NAME=ultra_food_db
DB_SSL_MODE=disable