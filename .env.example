# Database Configuration
# For local development (running without Docker): use "localhost"
# For Docker: use "postgres" (the service name from docker-compose.yml)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=fooddb

# Server Configuration
PORT=8080

# Authentication
JWT_SECRET=your-secret-key-change-in-production
